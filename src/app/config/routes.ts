export enum routes {
    home = '/',
    login = '/login',
    register = '/register',
    adminPannel = '/admin/pannel',
    chat = '/chat',
    feed = '/feed',
    logout = '/logout',
    settings = '/settings',
    settingsProfile = '/settings/profile',
}
